
kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-geoserver
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    org.geonode.instance: {{ .Release.Name }}-geoserver
  ports:
  - targetPort: 8080
    port: 8080
    name: http
  type: ClusterIP